// @ts-nocheck: not meant to be run
/**
 * Sample code to test with. Don't actually run this!
 * 
 * @todo I don't know, what *is* there to do?
 */

console.log("Use `deno task test` instead!");
Deno.exit(0);

// TODO: Do something here
function add(a: number, b: number): number {
    // TODO: Print message when strings are passed
    return a + b;
}

// @todo this isn't a todo actually

/**
 * Me!
 * 
 * @todo add favourite colour field
 * 
 * @property {string} name my name
 * @property {string} birthDate my birth date in RFC 9557 format
 */
interface Me {
    name: string;
    birthDate: string; 
}

/*
@todo shouldn't be triggered

TODO: ensure this IS triggered
*/

